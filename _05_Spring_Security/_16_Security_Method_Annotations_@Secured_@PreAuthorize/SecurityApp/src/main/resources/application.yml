spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID_SpringBoot_OAuth}
            client-secret: ${GOOGLE_CLIENT_SECRET_SpringBoot_OAuth}