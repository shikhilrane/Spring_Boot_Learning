RestClient (HTTP Client) :
    1. RestClient is use to get data that is not present in our server, but it is presented by some other 3rd party server.
    2. If we want to get data that is present in other 3rd party service, and then we can get the data by this RestAPI call called RestClient in Blocking Manner
    3. Blocking Manner : Your code gets blocked until your data gets return from service layer i.e. in synchronous manner
    4. RestClient will facilitate us to call any 3rd party service APIs to our own server or application
    5. Before RestClient, we used to have RestTemplate. So now RestClient is upgradation over RestTemplate because this provides modern way to deal with HTTP Services.
    6. We also have other HTTP client like WebClient that can be use to get data in Non-Blocking Manner. i.e. in asynchronous manner. This deals in flux for that we have to learn about web-flux.
    7. RestClient provides abstraction over lots of Http libraries, Also we can use those libraries as well, but in that case we have to handle a lot of stuff like how we are handling http request and how we are handling response, handling the error, and, etc. if we use those raw http libraries. So we use RestClient in place of those raw libraries.
    8. RestClient uses Jackson library to convert the data from JSON to Java Object and Java Object to JSON
    9. RestClient is already present in SpringBoot Web dependency so we don't have to add any external dependency

Building RestClient :
    RestClient restClient = RestClient.builder()                                // Builder to create RestClient object
        .baseUrl(BASE_URL)                                                      // Pass the base URL for all HTTP requests
        .defaultHeader(HttpHeaders.AUTHORIZATION,                               // Add default Authorization header
            encodeBasic(properties.getUsername(), properties.getPassword())     // Generates Basic base64(username:password)
        )                                                                       // Basic Auth header
        .build();                                                               // Build the RestClient object

Creating RestClient :
    Calling APIs from Week 2's Transform API Project
    Step 1 : Change the Port of any of the one project because both the servers can't run on same server.
    Step 2 : Create a config class called RestClientConfig
    step 3 : Configure the RestClient in config package
            @Bean
            @Qualifier("employeeRestClient")
            public RestClient getEmployeeServiceRestClient(){
                return RestClient.builder()
                        .baseUrl(BASE_URL)
                        .defaultHeader(CONTENT_TYPE, APPLICATION_JSON_VALUE)
                        .build();
            }
    Step 4 : Create EmployeeDto in which data should be traverse
    Step 5 : Create separate client package to create service for foreign APIs
    Step 6 : Create Controller to access Data from services

Error Handling in Rest Client :
    1. Use onStatus() in methods of Service
    2. Use 2 onStatus(), Because :
        1. 404 – client mistake / resource not found
            .onStatus(
                status -> status.value() == 404,
                (req, res) -> { throw new ResourceNotFoundException("Employee not found"); }
            )
            Notes : Use status.value() == 404, in all methods except create method because only 404 can be occurred in all method
                    Use status.is4xxClientError, in create method because there can 400, 401, 403, 409 so we use generic method here
        2. 5xx – server issue
            .onStatus(
                status -> status.is5xxServerError(),
                (req, res) -> { throw new RemoteServerException("Service down"); }
            )
            Notes : We use status.is5xxServerError(), everywhere to show server error because server could be anything
                    We can put 5xx error in RestClientConfig as well by using .defaultStatusHandler
        Also we can throw our custom errors using exceptions and advice
    3. 4xx and 5xx errors:
        4xx (Client Errors):
        400 Bad Request – request data is invalid
        401 Unauthorized – authentication missing or invalid
        403 Forbidden – access not allowed
        404 Not Found – resource does not exist
        405 Method Not Allowed – HTTP method not supported
        408 Request Timeout – client took too long to send request
        409 Conflict – request conflicts with existing data
        410 Gone – resource permanently removed
        415 Unsupported Media Type – wrong content type sent
        422 Unprocessable Entity – validation failed
        429 Too Many Requests – rate limit exceeded

        5xx (Server Errors):
        500 Internal Server Error – server failed unexpectedly
        501 Not Implemented – feature not supported by server
        502 Bad Gateway – invalid response from upstream server
        503 Service Unavailable – server temporarily down
        504 Gateway Timeout – upstream server did not respond
        507 Insufficient Storage – server out of storage
        508 Loop Detected – infinite request loop on server

Complete Flow ---->
        Configure Port -> Config RestClient -> ClientCto -> Service -> Controller